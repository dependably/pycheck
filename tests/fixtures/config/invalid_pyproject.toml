[build-system]
requires = "setuptools"

[project]
name = "_bad name!"
version = "1.0.x"
requires-python = "3.9"
dependencies = [
    "ok-package==1.0.0",
    "bad spec !!!",
    42,
]
keywords = "should-be-a-list"
